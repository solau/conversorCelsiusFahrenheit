--liquibase formatted sql

--changeset leandro:1
CREATE TABLE IF NOT EXISTS historico (
 ID BIGINT NOT NULL GENERATED BY DEFAULT AS IDENTITY CONSTRAINT historico_pkey PRIMARY KEY ,
 CELSIUS DOUBLE ,
 FAHRENHEIT DOUBLE ,
 CONVERTER_PARA VARCHAR(10),
 DATA_CONSULTA TIMESTAMP
);